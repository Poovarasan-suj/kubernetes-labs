apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  type: ClusterIP
  selector:
     app: {{.Chart.Name}}
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{.Values.containerPort}}
