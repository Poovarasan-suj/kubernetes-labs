apiVersion: v1
kind: Service
metadata:
  name: mydevops-service
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.labels.app }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.container.port }}
